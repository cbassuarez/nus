Throwaway experiments. Each directory is an independent Cargo project (not a
workspace member). See docs/SPIKES.md for what each must answer. Delete a spike
once its findings are recorded and its code has moved into crates/.
